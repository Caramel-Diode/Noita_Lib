/**
 * ### 🛠️ 将文件嵌入该位置
 * ---
 * 二进制文件将转为`base64`字符串
 * css 转为字符串
 * @param {String} path 目标文件路径 `#`前缀表示相对该文件的位置
 * @returns {String|meta_data} "字符串" 或 `元数据`
 */
const embed = path => {};
